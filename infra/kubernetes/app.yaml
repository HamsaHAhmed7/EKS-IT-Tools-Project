apiVersion: apps/v1
kind: Deployment
metadata:
  name: eks-it-tools
  labels:
    app: eks-it-tools
spec:
  replicas: 2
  selector:
    matchLabels:
      app: eks-it-tools
  template:
    metadata:
      labels:
        app: eks-it-tools
    spec:
      containers:
        - name: eks-it-tools
          image: 016873651140.dkr.ecr.eu-west-2.amazonaws.com/eks-it-tools:latest
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
